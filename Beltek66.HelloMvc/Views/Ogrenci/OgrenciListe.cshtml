@model List<Ogrenci>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Ad</th>
            <th>Soyad</th>
            <th>Yaş</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var ogr in Model)
        {
            <tr>
                <td>@ogr.Ad</td>
                <td>@ogr.Soyad</td>
                <td>@ogr.Yas</td>
                <td><a class="btn btn-sm btn-primary">Detay</a> <a class="btn btn-sm btn-danger">Sil</a></td>
            </tr>
        }
    </tbody>
</table>



